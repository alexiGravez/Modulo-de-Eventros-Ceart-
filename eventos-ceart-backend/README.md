# Calendario Backend (Node + Postgres + Docker)

## Requisitos
- Docker Desktop (Windows)
- (Opcional) Node 20+ si quieres correr sin Docker

## Levantar todo
```bash
docker compose up -d --build
